import React from 'react'
import { render } from '@testing-library/react'
import App from './App'

test('increments strike property of state by 1 until it reaches 2', () => {
    const component = render(<App />)
})
